# 数据库更新同步记录（hipp-or）

## 小程序对话（会话与消息表）

迁移文件：

- 建表：`hipp-api/alembic/versions_dev/d4f2a1b3c8e0_mp_chat_session_message.py`（`vadmin_chat_session`、`vadmin_chat_message`）。
- **无外键**：`hipp-api/alembic/versions_dev/f7a91b2c3d4e_mp_chat_drop_foreign_keys.py` 删除表上 FOREIGN KEY 约束，仅保留逻辑 ID 与普通索引；与 `doc/02-dr/04 小程序对话/小程序用户对话设计.md` 一致。

**顺序**：`80492c7c769a` → `f7a91b2c3d4e`（删 FK）→ `5e8d64b0274f`（全库 datetime 等 alter）。先删外键再跑可能含 `drop_index` 的 autogenerate，可避免 **1553**。

对 `vadmin_chat_*` 做 `alembic revision --autogenerate` 时若误生成 `drop_index`（外键列索引），MySQL 可能报 **1553**，合并前请删去此类语句。

```shell
# hipp-api 目录
./.venv/bin/python main.py migrate --env dev
# 生产环境
./.venv/bin/python main.py migrate --env pro
```

## 智能体管理（vadmin_agent）

```shell
# hipp-api 文件夹下，开发环境
./.venv/bin/python main.py migrate --env dev
# 生产环境
./.venv/bin/python main.py migrate --env pro
```

## 界面管理同步
在管理后台的系统管理→菜单管理中添加「智能客服」菜单（路径 /agent/manager，组件 views/Vadmin/AgentManager/AgentManager）
或直接使用已添加的 asyncRouterMap 静态路由
