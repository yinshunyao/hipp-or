# 关联文档

| 类型 | 文档路径 |
|:---|:---|
| 原始需求 | `hipp-or/doc/01-or/【系统管理】 增加数据导入导出功能.md` |
| 开发设计 | `hipp-or/doc/02-dr/05 系统管理/数据导入导出设计.md` |

# 测试范围

- 后端：`TABLES_ORDER` 唯一性、ZIP 缺少 `manifest.json` 时抛出明确业务异常。
- 前端：系统配置页出现「数据导入导出」Tab；导出触发下载、导入提交 FormData（自动化以手工/E2E 为主时可在后续迭代补 Playwright）。

# 测试用例

| 编号 | 前置条件 | 步骤 | 预期结果 | 自动化 |
|:---|:---|:---|:---|:---|
| TC-01 | 无 | 运行 `test_data_migration.py` 中单测 | 表清单无重复；缺 manifest 的 ZIP 校验失败 | pytest |
| TC-02 | 管理员已登录 | 打开系统配置 | 可见「数据导入导出」Tab | 建议 E2E |
| TC-03 | 管理员已登录 | 在导入导出 Tab 点击导出 | 下载 ZIP，内含 manifest 与 data 下 JSON | 手工/可选 E2E |
| TC-04 | 有效导出包 | 在测试库执行导入 | 数据与包一致，接口返回 `rows_inserted` 等摘要 | 手工/集成环境 |

# 自动化测试标识

- `hipp-or/hipp-api/test/test_apps/test_vadmin/test_system/test_data_migration.py`：单元级校验。
