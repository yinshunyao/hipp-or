# 关联文档

| 类型 | 文档路径 |
|:---|:---|
| 原始需求 | `hipp-or/doc/01-or/【后台管理】用户管理定制功能.md` |
| 开发设计 | `hipp-or/doc/02-dr/05 系统管理/后台用户管理定制设计.md` |

# 测试范围

- 后端：`user_tags` 计算规则、`is_system_created` / `is_blocked` 写入路径、登录与刷新 token 对拉黑的拦截、`PUT .../blocked` 权限与禁止项。
- 管理端：用户列表展示标签与拉黑列、拉黑/解除拉黑确认、删除二次确认（沿用原表格行为）。

# 测试用例

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 自动化 |
|:---|:---|:---|:---|:---|
| UM-01 | 无 | 对仅 `user_type=wechat` 且 `is_system_created=false` 的用户对象调用 `compute_user_tags` | 仅含「微信用户」 | pytest |
| UM-02 | 无 | 对 `is_system_created=true` 且 `user_type=system`、无微信字段 | 仅含「系统创建」 | pytest |
| UM-03 | 无 | 对 `is_system_created=true` 且 `user_type=wechat` | 含「系统创建」「微信用户」 | pytest |
| UM-04 | 集成环境可选 | 拉黑用户后密码登录 / 微信登录 / refresh | 返回拉黑相关错误 | 手动或后续 e2e |

# 自动化测试标识

- **pytest**：`hipp-or/hipp-api/test/test_apps/test_vadmin/test_auth/test_user_tags.py`（`compute_user_tags` 规则）。
